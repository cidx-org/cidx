# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
/cidx
bin/

# Test binary, built with `go test -c`
*.test

# Code coverage profiles and other test artifacts
*.out
coverage.*
*.coverprofile
profile.cov
coverage.html

# Dependency directories
vendor/

# Go workspace file
go.work
go.work.sum

# env file
.env

# Editor/IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
.claude/settings.local.json

# Scanner artifacts: the default --results directory of `security vuln prune`,
# `security baseline` and `preset scan-verdicts`. Downloading a workflow's
# artifacts here and committing them swept 43 files and 201k lines into a branch
# (#323), and gitleaks — which scans every ref — then failed the Security job of
# every CI run in the repository on CVE text that pattern-matches an API token.
scan-results/

# Internal notes
.cidx/ideas/

# Tag staging files: local hand-off between `release tag prepare` and
# `release tag create`, never committed (release notes are, so they stay)
.cidx/tag-version
.cidx/tag-message

#AI
.serena
.claude