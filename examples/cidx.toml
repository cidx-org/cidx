# CIDX Configuration - Minimal CI
#
# A small starting point for running the same checks locally and in CI.

[security]
containers = ["trivy", "gitleaks"]

[code]
containers = ["prettier"]

[pipelines.ci]
phases = ["security", "code"]
