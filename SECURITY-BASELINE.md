# Security baseline

<!-- Generated by `cidx security baseline`. Do not edit by hand. -->

What the built-in preset catalogue ships: every container image it runs, pinned
by digest, what those images carry, and every HIGH/CRITICAL finding accepted on
them — with the reason it was accepted and the date that acceptance has to be
argued again.

Carried and accepted are different numbers, and both are stated. A file saying
only what is accepted read "0 accepted findings" on a catalogue carrying 596.
Accepted is a subset of carried: accepting a finding records that it was looked
at, it does not take it out of the image — so the carried count includes what
the audit's ignore file removes from its own scan results (#310).

This file is committed, so its diff is the history of what the catalogue
delivers, and it carries no generation date: the same inputs produce the same
bytes, and a changed line means something actually changed.

Only the built-in catalogue is described here. Presets your own `presets.toml`
declares are yours, and CIDX makes no claim about them (guardrail 1).

An entry past its expiry date waives nothing until it is reviewed; `cidx
security vuln check` is what reports those, and `cidx security vuln prune`
reports the ones no catalogue image carries any more.

**21 images. 170 accepted HIGH/CRITICAL finding(s) across 17 of them.**

## What the images carry

The catalogue carries **664** HIGH/CRITICAL findings, counted per image — the
same CVE on five images is five repins. They split into four populations:

| Population | Count | What it means |
| ---------- | ----- | ------------- |
| Go stdlib in a CLI binary | 121 | Exempt by class: it goes away when the publisher recompiles, and `net/http` is unreachable in a tool that opens no listener. |
| Kernel headers | 84 | Exempt by class: the kernel is the host's, not the container's. The scanner flags the headers package for its version string. |
| Fixed upstream | 306 | A fix exists. This is the images' age, not a decision — an exception must never be written for one. |
| **Needing triage** | **153** | No fix at any version, and not exempt. The only population an exception is the right instrument for. |

None of them is in CISA KEV. The highest EPSS score seen is 0.10. Both are reported
for a human to read; neither gates anything.

## Images

The **Base** column is the distribution the image is built on, as the scanners
report it. It is here because it decides whether the findings above can ever
go away: once a base stops being supported, its packages receive no further
updates and every finding on that image is permanent, however fresh the tag
is. `none` is an image with no distribution underneath it — a scratch or
static build — which is an answer, not a gap.

The date that support ends is deliberately **not** in this file. It is
relative to the day you read it and it comes from a third party, so it would
change these lines without anything changing about the catalogue — the same
reason there is no generation date. `cidx security baseline` prints it, and
the daily security audit reports it.

| Image | Presets | Base | Carried HIGH/CRITICAL | Accepted |
| ----- | ------- | ---- | --------------------- | -------- |
| `buildpack-deps:trixie-curl@sha256:ef554489e8f8b0245ef3cb2742b4c6b6e53c43bd64366b13f624982c854af2eb` | cargo-audit | debian 13.6 | 48 | 44 |
| `commitizen/commitizen:4.16.5@sha256:451d0150ed7804c51cf201b2e9bd3f351867badbb7dc98f195cad740fdeaabaa` | commitizen | alpine 3.24.1 | 9 | 6 |
| `commitlint/commitlint:21.2.1@sha256:f4b38082bec66b4cd1b37a0357145dded515c4468e2b339ec9ba31f86461f6b9` | commitlint | alpine 3.24.1 | 27 | 1 |
| `dhi.io/alpine-base:3.24@sha256:84a21d3dfb87eb1c0bf1b532350f2aa0bf7d0df6246c398cc5c14b29002b7310` | test-hot-reload | alpine 3.24 | 1 | 1 |
| `dhi.io/docker:29-cli@sha256:5962df1dad91fbf81be7c795c00d6137c6a46ac28acada6af9f0af7c92133966` | docker-buildx | debian 13.6 | 31 | 8 |
| `dhi.io/golang:1.26.5-alpine-dev@sha256:29fe0a7d2a5ab0c236fbde3a7f63801755585ed260b6f2f564e831c92bfa9f34` | go-build, go-mod-tidy, go-test, godog, gofmt, govulncheck | alpine 3.24 | 7 | 3 |
| `dhi.io/python:3.13.14-alpine-dev@sha256:8c3820102033dff6a1e5de0fad001747c7b0979c372d6ffc1ac95dd0e03eed82` | bandit, mypy, pip-audit, pytest, python-build, twine | alpine 3.24 | 6 | 6 |
| `dhi.io/trivy:0.71@sha256:fd727baaf00f823f59c2936f2c797c61750e2440b736c5b787722c18dd9518aa` | trivy | debian 13.6 | 3 | none |
| `ghcr.io/ansible/community-ansible-dev-tools:v26.7.1@sha256:4d4db3e75c48ce64763d26adbca58ff3f8b93a8ddae785373ac973b4f20a7d92` | ansible-galaxy-build, ansible-galaxy-publish, ansible-lint, ansible-syntax, ansible-test, molecule, yamllint | fedora 44 | 111 | 12 |
| `ghcr.io/astral-sh/ruff:0.8.2@sha256:84b0ad0023906db70b759b3b29e455dad0638159bf2de2b95086db1ab175917b` | ruff | none | 0 | none |
| `ghcr.io/osscontainertools/kaniko:v1.28.0@sha256:f072b11159668f4d11ba3a38d489db7ff18eb961b8140de91a437c71c1747583` | kaniko | none | 21 | none |
| `ghcr.io/probatum-org/probatum:0.8.0@sha256:b1f7a2bb4c1a44a29ee62b92fca4c6f6fb60fbc56843421dcd65c430d3697f51` | probatum | alpine 3.24.1 | 1 | 1 |
| `ghcr.io/securego/gosec:2.28.0@sha256:4342ad119a7c69f3f4e4ce78d81ba183dc774a70a7a4c6eeb15fe9e511f214f0` | gosec | alpine 3.24.1 | 24 | 6 |
| `golangci/golangci-lint:v2.12.2-alpine@sha256:91b27804074a0bacea298707f016911e60cf0cdbc6c7bf5ccacb5f0606d18d60` | golangci-lint | alpine 3.23.4 | 62 | 9 |
| `goreleaser/goreleaser:v2.17.0@sha256:054eefd282c02233a2556ce2d1a60cd2f51dc565ffc2520dc38b5deb4dd1ad30` | goreleaser | alpine 3.24.1 | 53 | 8 |
| `jauderho/prettier:3.9.4@sha256:b233d9c847048561850a1a107e35318b28361cd9a850eac7edcd5d8bfd82cea2` | prettier | alpine 3.24.1 | 8 | 7 |
| `koalaman/shellcheck:stable@sha256:bb596a0d169b85ddd81d8b6d3a2ff6d5baf5fca10b97f575ebc647c3dff62b3d` | shellcheck | none | 0 | none |
| `maniator/gh:v2.95@sha256:c7e862eeb468003aba87847a03e214a21638cd898130d6e5894f019aec091913` | gh-release | alpine 3.24.1 | 22 | 1 |
| `pyfound/black:26.5.1@sha256:bcdafe3e6a60fd181fde19859f7ee4c498557f03bb30af3fa880f502a66e5b5f` | black | debian 13.4 | 54 | 22 |
| `rust:1.97.1-slim@sha256:3b2879047d42784ca9403ad20c51ed3df361a50f1df96f5777d39b4e33aa65cd` | cargo-build, cargo-publish, cargo-test, clippy, rustfmt | debian 13.6 | 110 | 34 |
| `zricethezav/gitleaks:v8.30.1@sha256:c00b6bd0aeb3071cbcb79009cb16a60dd9e0a7c60e2be9ab65d25e6bc8abbb7f` | gitleaks | alpine 3.22.3 | 66 | 1 |

## Accepted findings

| Image | CVE | Severity | Status | Expires | Justification |
| ----- | --- | -------- | ------ | ------- | ------------- |
| `buildpack-deps:trixie-curl@sha256:ef554489e8f8b0245ef3cb2742b4c6b6e53c43bd64366b13f624982c854af2eb` | CVE-2025-69720 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `buildpack-deps:trixie-curl@sha256:ef554489e8f8b0245ef3cb2742b4c6b6e53c43bd64366b13f624982c854af2eb` | CVE-2026-10536 | HIGH | accepted-risk | 2026-11-18 | This package IS what the preset runs, so the 'nothing calls it' argument does not apply. What bounds it instead is the input: the tool reads the repository's own source, or fetches a URL the preset itself fixes -- never bytes chosen by anyone else. Weaker than the inert case and stated as such. No fix at any version. |
| `buildpack-deps:trixie-curl@sha256:ef554489e8f8b0245ef3cb2742b4c6b6e53c43bd64366b13f624982c854af2eb` | CVE-2026-11822 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `buildpack-deps:trixie-curl@sha256:ef554489e8f8b0245ef3cb2742b4c6b6e53c43bd64366b13f624982c854af2eb` | CVE-2026-11824 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `buildpack-deps:trixie-curl@sha256:ef554489e8f8b0245ef3cb2742b4c6b6e53c43bd64366b13f624982c854af2eb` | CVE-2026-11856 | HIGH | accepted-risk | 2026-11-18 | This package IS what the preset runs, so the 'nothing calls it' argument does not apply. What bounds it instead is the input: the tool reads the repository's own source, or fetches a URL the preset itself fixes -- never bytes chosen by anyone else. Weaker than the inert case and stated as such. No fix at any version. |
| `buildpack-deps:trixie-curl@sha256:ef554489e8f8b0245ef3cb2742b4c6b6e53c43bd64366b13f624982c854af2eb` | CVE-2026-12064 | HIGH | accepted-risk | 2026-11-18 | This package IS what the preset runs, so the 'nothing calls it' argument does not apply. What bounds it instead is the input: the tool reads the repository's own source, or fetches a URL the preset itself fixes -- never bytes chosen by anyone else. Weaker than the inert case and stated as such. No fix at any version. |
| `buildpack-deps:trixie-curl@sha256:ef554489e8f8b0245ef3cb2742b4c6b6e53c43bd64366b13f624982c854af2eb` | CVE-2026-12087 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `buildpack-deps:trixie-curl@sha256:ef554489e8f8b0245ef3cb2742b4c6b6e53c43bd64366b13f624982c854af2eb` | CVE-2026-13221 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `buildpack-deps:trixie-curl@sha256:ef554489e8f8b0245ef3cb2742b4c6b6e53c43bd64366b13f624982c854af2eb` | CVE-2026-14456 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `buildpack-deps:trixie-curl@sha256:ef554489e8f8b0245ef3cb2742b4c6b6e53c43bd64366b13f624982c854af2eb` | CVE-2026-24882 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `buildpack-deps:trixie-curl@sha256:ef554489e8f8b0245ef3cb2742b4c6b6e53c43bd64366b13f624982c854af2eb` | CVE-2026-41992 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `buildpack-deps:trixie-curl@sha256:ef554489e8f8b0245ef3cb2742b4c6b6e53c43bd64366b13f624982c854af2eb` | CVE-2026-42496 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `buildpack-deps:trixie-curl@sha256:ef554489e8f8b0245ef3cb2742b4c6b6e53c43bd64366b13f624982c854af2eb` | CVE-2026-42497 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `buildpack-deps:trixie-curl@sha256:ef554489e8f8b0245ef3cb2742b4c6b6e53c43bd64366b13f624982c854af2eb` | CVE-2026-48959 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `buildpack-deps:trixie-curl@sha256:ef554489e8f8b0245ef3cb2742b4c6b6e53c43bd64366b13f624982c854af2eb` | CVE-2026-48961 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `buildpack-deps:trixie-curl@sha256:ef554489e8f8b0245ef3cb2742b4c6b6e53c43bd64366b13f624982c854af2eb` | CVE-2026-48962 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `buildpack-deps:trixie-curl@sha256:ef554489e8f8b0245ef3cb2742b4c6b6e53c43bd64366b13f624982c854af2eb` | CVE-2026-5435 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `buildpack-deps:trixie-curl@sha256:ef554489e8f8b0245ef3cb2742b4c6b6e53c43bd64366b13f624982c854af2eb` | CVE-2026-54369 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `buildpack-deps:trixie-curl@sha256:ef554489e8f8b0245ef3cb2742b4c6b6e53c43bd64366b13f624982c854af2eb` | CVE-2026-54370 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `buildpack-deps:trixie-curl@sha256:ef554489e8f8b0245ef3cb2742b4c6b6e53c43bd64366b13f624982c854af2eb` | CVE-2026-5450 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `buildpack-deps:trixie-curl@sha256:ef554489e8f8b0245ef3cb2742b4c6b6e53c43bd64366b13f624982c854af2eb` | CVE-2026-57432 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `buildpack-deps:trixie-curl@sha256:ef554489e8f8b0245ef3cb2742b4c6b6e53c43bd64366b13f624982c854af2eb` | CVE-2026-57433 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `buildpack-deps:trixie-curl@sha256:ef554489e8f8b0245ef3cb2742b4c6b6e53c43bd64366b13f624982c854af2eb` | CVE-2026-58050 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `buildpack-deps:trixie-curl@sha256:ef554489e8f8b0245ef3cb2742b4c6b6e53c43bd64366b13f624982c854af2eb` | CVE-2026-58051 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `buildpack-deps:trixie-curl@sha256:ef554489e8f8b0245ef3cb2742b4c6b6e53c43bd64366b13f624982c854af2eb` | CVE-2026-58469 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `buildpack-deps:trixie-curl@sha256:ef554489e8f8b0245ef3cb2742b4c6b6e53c43bd64366b13f624982c854af2eb` | CVE-2026-58471 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `buildpack-deps:trixie-curl@sha256:ef554489e8f8b0245ef3cb2742b4c6b6e53c43bd64366b13f624982c854af2eb` | CVE-2026-58472 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `buildpack-deps:trixie-curl@sha256:ef554489e8f8b0245ef3cb2742b4c6b6e53c43bd64366b13f624982c854af2eb` | CVE-2026-5928 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `buildpack-deps:trixie-curl@sha256:ef554489e8f8b0245ef3cb2742b4c6b6e53c43bd64366b13f624982c854af2eb` | CVE-2026-66032 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `buildpack-deps:trixie-curl@sha256:ef554489e8f8b0245ef3cb2742b4c6b6e53c43bd64366b13f624982c854af2eb` | CVE-2026-66033 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `buildpack-deps:trixie-curl@sha256:ef554489e8f8b0245ef3cb2742b4c6b6e53c43bd64366b13f624982c854af2eb` | CVE-2026-66034 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `buildpack-deps:trixie-curl@sha256:ef554489e8f8b0245ef3cb2742b4c6b6e53c43bd64366b13f624982c854af2eb` | CVE-2026-66035 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `buildpack-deps:trixie-curl@sha256:ef554489e8f8b0245ef3cb2742b4c6b6e53c43bd64366b13f624982c854af2eb` | CVE-2026-7017 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `buildpack-deps:trixie-curl@sha256:ef554489e8f8b0245ef3cb2742b4c6b6e53c43bd64366b13f624982c854af2eb` | CVE-2026-8286 | HIGH | accepted-risk | 2026-11-18 | This package IS what the preset runs, so the 'nothing calls it' argument does not apply. What bounds it instead is the input: the tool reads the repository's own source, or fetches a URL the preset itself fixes -- never bytes chosen by anyone else. Weaker than the inert case and stated as such. No fix at any version. |
| `buildpack-deps:trixie-curl@sha256:ef554489e8f8b0245ef3cb2742b4c6b6e53c43bd64366b13f624982c854af2eb` | CVE-2026-8376 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `buildpack-deps:trixie-curl@sha256:ef554489e8f8b0245ef3cb2742b4c6b6e53c43bd64366b13f624982c854af2eb` | CVE-2026-8458 | HIGH | accepted-risk | 2026-11-18 | This package IS what the preset runs, so the 'nothing calls it' argument does not apply. What bounds it instead is the input: the tool reads the repository's own source, or fetches a URL the preset itself fixes -- never bytes chosen by anyone else. Weaker than the inert case and stated as such. No fix at any version. |
| `buildpack-deps:trixie-curl@sha256:ef554489e8f8b0245ef3cb2742b4c6b6e53c43bd64366b13f624982c854af2eb` | CVE-2026-8924 | HIGH | accepted-risk | 2026-11-18 | This package IS what the preset runs, so the 'nothing calls it' argument does not apply. What bounds it instead is the input: the tool reads the repository's own source, or fetches a URL the preset itself fixes -- never bytes chosen by anyone else. Weaker than the inert case and stated as such. No fix at any version. |
| `buildpack-deps:trixie-curl@sha256:ef554489e8f8b0245ef3cb2742b4c6b6e53c43bd64366b13f624982c854af2eb` | CVE-2026-8926 | HIGH | accepted-risk | 2026-11-18 | This package IS what the preset runs, so the 'nothing calls it' argument does not apply. What bounds it instead is the input: the tool reads the repository's own source, or fetches a URL the preset itself fixes -- never bytes chosen by anyone else. Weaker than the inert case and stated as such. No fix at any version. |
| `buildpack-deps:trixie-curl@sha256:ef554489e8f8b0245ef3cb2742b4c6b6e53c43bd64366b13f624982c854af2eb` | CVE-2026-8927 | HIGH | accepted-risk | 2026-11-18 | This package IS what the preset runs, so the 'nothing calls it' argument does not apply. What bounds it instead is the input: the tool reads the repository's own source, or fetches a URL the preset itself fixes -- never bytes chosen by anyone else. Weaker than the inert case and stated as such. No fix at any version. |
| `buildpack-deps:trixie-curl@sha256:ef554489e8f8b0245ef3cb2742b4c6b6e53c43bd64366b13f624982c854af2eb` | CVE-2026-8932 | HIGH | accepted-risk | 2026-11-18 | This package IS what the preset runs, so the 'nothing calls it' argument does not apply. What bounds it instead is the input: the tool reads the repository's own source, or fetches a URL the preset itself fixes -- never bytes chosen by anyone else. Weaker than the inert case and stated as such. No fix at any version. |
| `buildpack-deps:trixie-curl@sha256:ef554489e8f8b0245ef3cb2742b4c6b6e53c43bd64366b13f624982c854af2eb` | CVE-2026-9079 | HIGH | accepted-risk | 2026-11-18 | This package IS what the preset runs, so the 'nothing calls it' argument does not apply. What bounds it instead is the input: the tool reads the repository's own source, or fetches a URL the preset itself fixes -- never bytes chosen by anyone else. Weaker than the inert case and stated as such. No fix at any version. |
| `buildpack-deps:trixie-curl@sha256:ef554489e8f8b0245ef3cb2742b4c6b6e53c43bd64366b13f624982c854af2eb` | CVE-2026-9080 | HIGH | accepted-risk | 2026-11-18 | This package IS what the preset runs, so the 'nothing calls it' argument does not apply. What bounds it instead is the input: the tool reads the repository's own source, or fetches a URL the preset itself fixes -- never bytes chosen by anyone else. Weaker than the inert case and stated as such. No fix at any version. |
| `buildpack-deps:trixie-curl@sha256:ef554489e8f8b0245ef3cb2742b4c6b6e53c43bd64366b13f624982c854af2eb` | CVE-2026-9538 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `buildpack-deps:trixie-curl@sha256:ef554489e8f8b0245ef3cb2742b4c6b6e53c43bd64366b13f624982c854af2eb` | CVE-2026-9545 | HIGH | accepted-risk | 2026-11-18 | This package IS what the preset runs, so the 'nothing calls it' argument does not apply. What bounds it instead is the input: the tool reads the repository's own source, or fetches a URL the preset itself fixes -- never bytes chosen by anyone else. Weaker than the inert case and stated as such. No fix at any version. |
| `commitizen/commitizen:4.16.5@sha256:451d0150ed7804c51cf201b2e9bd3f351867badbb7dc98f195cad740fdeaabaa` | CVE-2025-69649 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `commitizen/commitizen:4.16.5@sha256:451d0150ed7804c51cf201b2e9bd3f351867badbb7dc98f195cad740fdeaabaa` | CVE-2025-69650 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `commitizen/commitizen:4.16.5@sha256:451d0150ed7804c51cf201b2e9bd3f351867badbb7dc98f195cad740fdeaabaa` | CVE-2026-14456 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `commitizen/commitizen:4.16.5@sha256:451d0150ed7804c51cf201b2e9bd3f351867badbb7dc98f195cad740fdeaabaa` | CVE-2026-3441 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `commitizen/commitizen:4.16.5@sha256:451d0150ed7804c51cf201b2e9bd3f351867badbb7dc98f195cad740fdeaabaa` | CVE-2026-3442 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `commitizen/commitizen:4.16.5@sha256:451d0150ed7804c51cf201b2e9bd3f351867badbb7dc98f195cad740fdeaabaa` | CVE-2026-6846 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `commitlint/commitlint:21.2.1@sha256:f4b38082bec66b4cd1b37a0357145dded515c4468e2b339ec9ba31f86461f6b9` | CVE-2026-14456 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `dhi.io/alpine-base:3.24@sha256:84a21d3dfb87eb1c0bf1b532350f2aa0bf7d0df6246c398cc5c14b29002b7310` | CVE-2026-14456 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `dhi.io/docker:29-cli@sha256:5962df1dad91fbf81be7c795c00d6137c6a46ac28acada6af9f0af7c92133966` | CVE-2026-14456 | HIGH | accepted-risk | 2026-11-18 | The preset runs docker buildx build; the buildx plugin is static and the compose plugin is never invoked. The docker/docker CVEs are daemon-side handlers absent from both client plugins; the daemon is the host's. glibc is linked by the Go CLI, and the flagged paths are not the Go runtime's; the image's OpenSSL is linked by nothing the preset runs. No fix at any version. |
| `dhi.io/docker:29-cli@sha256:5962df1dad91fbf81be7c795c00d6137c6a46ac28acada6af9f0af7c92133966` | CVE-2026-41567 | HIGH | accepted-risk | 2026-11-18 | The preset runs docker buildx build; the buildx plugin is static and the compose plugin is never invoked. The docker/docker CVEs are daemon-side handlers absent from both client plugins; the daemon is the host's. glibc is linked by the Go CLI, and the flagged paths are not the Go runtime's; the image's OpenSSL is linked by nothing the preset runs. No fix at any version. |
| `dhi.io/docker:29-cli@sha256:5962df1dad91fbf81be7c795c00d6137c6a46ac28acada6af9f0af7c92133966` | CVE-2026-42306 | HIGH | accepted-risk | 2026-11-18 | The preset runs docker buildx build; the buildx plugin is static and the compose plugin is never invoked. The docker/docker CVEs are daemon-side handlers absent from both client plugins; the daemon is the host's. glibc is linked by the Go CLI, and the flagged paths are not the Go runtime's; the image's OpenSSL is linked by nothing the preset runs. No fix at any version. |
| `dhi.io/docker:29-cli@sha256:5962df1dad91fbf81be7c795c00d6137c6a46ac28acada6af9f0af7c92133966` | CVE-2026-5435 | HIGH | accepted-risk | 2026-11-18 | The preset runs docker buildx build; the buildx plugin is static and the compose plugin is never invoked. The docker/docker CVEs are daemon-side handlers absent from both client plugins; the daemon is the host's. glibc is linked by the Go CLI, and the flagged paths are not the Go runtime's; the image's OpenSSL is linked by nothing the preset runs. No fix at any version. |
| `dhi.io/docker:29-cli@sha256:5962df1dad91fbf81be7c795c00d6137c6a46ac28acada6af9f0af7c92133966` | CVE-2026-5450 | HIGH | accepted-risk | 2026-11-18 | The preset runs docker buildx build; the buildx plugin is static and the compose plugin is never invoked. The docker/docker CVEs are daemon-side handlers absent from both client plugins; the daemon is the host's. glibc is linked by the Go CLI, and the flagged paths are not the Go runtime's; the image's OpenSSL is linked by nothing the preset runs. No fix at any version. |
| `dhi.io/docker:29-cli@sha256:5962df1dad91fbf81be7c795c00d6137c6a46ac28acada6af9f0af7c92133966` | CVE-2026-5928 | HIGH | accepted-risk | 2026-11-18 | The preset runs docker buildx build; the buildx plugin is static and the compose plugin is never invoked. The docker/docker CVEs are daemon-side handlers absent from both client plugins; the daemon is the host's. glibc is linked by the Go CLI, and the flagged paths are not the Go runtime's; the image's OpenSSL is linked by nothing the preset runs. No fix at any version. |
| `dhi.io/docker:29-cli@sha256:5962df1dad91fbf81be7c795c00d6137c6a46ac28acada6af9f0af7c92133966` | GHSA-RG2X-37C3-W2RH | HIGH | accepted-risk | 2026-11-18 | The preset runs docker buildx build; the buildx plugin is static and the compose plugin is never invoked. The docker/docker CVEs are daemon-side handlers absent from both client plugins; the daemon is the host's. glibc is linked by the Go CLI, and the flagged paths are not the Go runtime's; the image's OpenSSL is linked by nothing the preset runs. No fix at any version. |
| `dhi.io/docker:29-cli@sha256:5962df1dad91fbf81be7c795c00d6137c6a46ac28acada6af9f0af7c92133966` | GHSA-X86F-5XW2-FM2R | HIGH | accepted-risk | 2026-11-18 | The preset runs docker buildx build; the buildx plugin is static and the compose plugin is never invoked. The docker/docker CVEs are daemon-side handlers absent from both client plugins; the daemon is the host's. glibc is linked by the Go CLI, and the flagged paths are not the Go runtime's; the image's OpenSSL is linked by nothing the preset runs. No fix at any version. |
| `dhi.io/golang:1.26.5-alpine-dev@sha256:29fe0a7d2a5ab0c236fbde3a7f63801755585ed260b6f2f564e831c92bfa9f34` | CVE-2026-14456 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `dhi.io/golang:1.26.5-alpine-dev@sha256:29fe0a7d2a5ab0c236fbde3a7f63801755585ed260b6f2f564e831c92bfa9f34` | CVE-2026-3441 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `dhi.io/golang:1.26.5-alpine-dev@sha256:29fe0a7d2a5ab0c236fbde3a7f63801755585ed260b6f2f564e831c92bfa9f34` | CVE-2026-3442 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `dhi.io/python:3.13.14-alpine-dev@sha256:8c3820102033dff6a1e5de0fad001747c7b0979c372d6ffc1ac95dd0e03eed82` | CVE-2026-11940 | HIGH | accepted-risk | 2026-11-18 | tarfile and html.parser are exercised by pip on sdists and the simple index, for pip-audit and the installs python-build and pytest perform; twine reads the dist/ the repository built. The input is the repository's own code and PyPI's responses for the packages it pins; the flaws are extraction-filter bypass and CPU exhaustion, not code execution. No fix at any version. |
| `dhi.io/python:3.13.14-alpine-dev@sha256:8c3820102033dff6a1e5de0fad001747c7b0979c372d6ffc1ac95dd0e03eed82` | CVE-2026-11972 | HIGH | accepted-risk | 2026-11-18 | tarfile and html.parser are exercised by pip on sdists and the simple index, for pip-audit and the installs python-build and pytest perform; twine reads the dist/ the repository built. The input is the repository's own code and PyPI's responses for the packages it pins; the flaws are extraction-filter bypass and CPU exhaustion, not code execution. No fix at any version. |
| `dhi.io/python:3.13.14-alpine-dev@sha256:8c3820102033dff6a1e5de0fad001747c7b0979c372d6ffc1ac95dd0e03eed82` | CVE-2026-14456 | HIGH | accepted-risk | 2026-11-18 | The interpreter loads OpenSSL for TLS to PyPI, and CVE-2026-14456 is in the QUIC server listener, a path no client use reaches; binutils is reached only by an sdist build of an object the project's dependency tree chose. No fix at any version. |
| `dhi.io/python:3.13.14-alpine-dev@sha256:8c3820102033dff6a1e5de0fad001747c7b0979c372d6ffc1ac95dd0e03eed82` | CVE-2026-15308 | HIGH | accepted-risk | 2026-11-18 | tarfile and html.parser are exercised by pip on sdists and the simple index, for pip-audit and the installs python-build and pytest perform; twine reads the dist/ the repository built. The input is the repository's own code and PyPI's responses for the packages it pins; the flaws are extraction-filter bypass and CPU exhaustion, not code execution. No fix at any version. |
| `dhi.io/python:3.13.14-alpine-dev@sha256:8c3820102033dff6a1e5de0fad001747c7b0979c372d6ffc1ac95dd0e03eed82` | CVE-2026-3441 | HIGH | accepted-risk | 2026-11-18 | The interpreter loads OpenSSL for TLS to PyPI, and CVE-2026-14456 is in the QUIC server listener, a path no client use reaches; binutils is reached only by an sdist build of an object the project's dependency tree chose. No fix at any version. |
| `dhi.io/python:3.13.14-alpine-dev@sha256:8c3820102033dff6a1e5de0fad001747c7b0979c372d6ffc1ac95dd0e03eed82` | CVE-2026-3442 | HIGH | accepted-risk | 2026-11-18 | The interpreter loads OpenSSL for TLS to PyPI, and CVE-2026-14456 is in the QUIC server listener, a path no client use reaches; binutils is reached only by an sdist build of an object the project's dependency tree chose. No fix at any version. |
| `ghcr.io/ansible/community-ansible-dev-tools:v26.7.1@sha256:4d4db3e75c48ce64763d26adbca58ff3f8b93a8ddae785373ac973b4f20a7d92` | CVE-2024-25621 | HIGH | awaiting-upstream | 2026-09-06 | runc, containerd, docker/docker and selinux are vendored into /usr/local/bin/kubectl, which no preset invokes; the vulnerable paths need a container to be run by that binary, and kubectl runs none. Fixed versions exist at module level and reach us only when the image ships a rebuilt kubectl (v26.7.1, the current latest, does not). Decided 2026-08-22: if the review date passes with no such image, this becomes accepted-risk under the same model -- the blast radius, if inertness is wrong, includes the Docker socket and the Galaxy token -- rather than disabling seven presets over an uninvoked library. |
| `ghcr.io/ansible/community-ansible-dev-tools:v26.7.1@sha256:4d4db3e75c48ce64763d26adbca58ff3f8b93a8ddae785373ac973b4f20a7d92` | CVE-2025-31133 | HIGH | awaiting-upstream | 2026-09-06 | runc, containerd, docker/docker and selinux are vendored into /usr/local/bin/kubectl, which no preset invokes; the vulnerable paths need a container to be run by that binary, and kubectl runs none. Fixed versions exist at module level and reach us only when the image ships a rebuilt kubectl (v26.7.1, the current latest, does not). Decided 2026-08-22: if the review date passes with no such image, this becomes accepted-risk under the same model -- the blast radius, if inertness is wrong, includes the Docker socket and the Galaxy token -- rather than disabling seven presets over an uninvoked library. |
| `ghcr.io/ansible/community-ansible-dev-tools:v26.7.1@sha256:4d4db3e75c48ce64763d26adbca58ff3f8b93a8ddae785373ac973b4f20a7d92` | CVE-2025-52565 | HIGH | awaiting-upstream | 2026-09-06 | runc, containerd, docker/docker and selinux are vendored into /usr/local/bin/kubectl, which no preset invokes; the vulnerable paths need a container to be run by that binary, and kubectl runs none. Fixed versions exist at module level and reach us only when the image ships a rebuilt kubectl (v26.7.1, the current latest, does not). Decided 2026-08-22: if the review date passes with no such image, this becomes accepted-risk under the same model -- the blast radius, if inertness is wrong, includes the Docker socket and the Galaxy token -- rather than disabling seven presets over an uninvoked library. |
| `ghcr.io/ansible/community-ansible-dev-tools:v26.7.1@sha256:4d4db3e75c48ce64763d26adbca58ff3f8b93a8ddae785373ac973b4f20a7d92` | CVE-2025-52881 | HIGH | awaiting-upstream | 2026-09-06 | runc, containerd, docker/docker and selinux are vendored into /usr/local/bin/kubectl, which no preset invokes; the vulnerable paths need a container to be run by that binary, and kubectl runs none. Fixed versions exist at module level and reach us only when the image ships a rebuilt kubectl (v26.7.1, the current latest, does not). Decided 2026-08-22: if the review date passes with no such image, this becomes accepted-risk under the same model -- the blast radius, if inertness is wrong, includes the Docker socket and the Galaxy token -- rather than disabling seven presets over an uninvoked library. |
| `ghcr.io/ansible/community-ansible-dev-tools:v26.7.1@sha256:4d4db3e75c48ce64763d26adbca58ff3f8b93a8ddae785373ac973b4f20a7d92` | CVE-2026-41567 | HIGH | awaiting-upstream | 2026-09-06 | runc, containerd, docker/docker and selinux are vendored into /usr/local/bin/kubectl, which no preset invokes; the vulnerable paths need a container to be run by that binary, and kubectl runs none. Fixed versions exist at module level and reach us only when the image ships a rebuilt kubectl (v26.7.1, the current latest, does not). Decided 2026-08-22: if the review date passes with no such image, this becomes accepted-risk under the same model -- the blast radius, if inertness is wrong, includes the Docker socket and the Galaxy token -- rather than disabling seven presets over an uninvoked library. |
| `ghcr.io/ansible/community-ansible-dev-tools:v26.7.1@sha256:4d4db3e75c48ce64763d26adbca58ff3f8b93a8ddae785373ac973b4f20a7d92` | CVE-2026-42306 | HIGH | awaiting-upstream | 2026-09-06 | runc, containerd, docker/docker and selinux are vendored into /usr/local/bin/kubectl, which no preset invokes; the vulnerable paths need a container to be run by that binary, and kubectl runs none. Fixed versions exist at module level and reach us only when the image ships a rebuilt kubectl (v26.7.1, the current latest, does not). Decided 2026-08-22: if the review date passes with no such image, this becomes accepted-risk under the same model -- the blast radius, if inertness is wrong, includes the Docker socket and the Galaxy token -- rather than disabling seven presets over an uninvoked library. |
| `ghcr.io/ansible/community-ansible-dev-tools:v26.7.1@sha256:4d4db3e75c48ce64763d26adbca58ff3f8b93a8ddae785373ac973b4f20a7d92` | GHSA-M68Q-4HQR-MC6F | HIGH | accepted-risk | 2026-11-18 | podman is a tool a preset may run; what bounds it is the input -- the repository's own molecule scenarios -- and the socket it reaches is the host's. No fix at any version. |
| `ghcr.io/ansible/community-ansible-dev-tools:v26.7.1@sha256:4d4db3e75c48ce64763d26adbca58ff3f8b93a8ddae785373ac973b4f20a7d92` | GHSA-RPCC-P8XM-RC6P | HIGH | accepted-risk | 2026-11-18 | podman is a tool a preset may run; what bounds it is the input -- the repository's own molecule scenarios -- and the socket it reaches is the host's. No fix at any version. |
| `ghcr.io/ansible/community-ansible-dev-tools:v26.7.1@sha256:4d4db3e75c48ce64763d26adbca58ff3f8b93a8ddae785373ac973b4f20a7d92` | GHSA-cgrx-mc8f-2prm | HIGH | awaiting-upstream | 2026-09-06 | runc, containerd, docker/docker and selinux are vendored into /usr/local/bin/kubectl, which no preset invokes; the vulnerable paths need a container to be run by that binary, and kubectl runs none. Fixed versions exist at module level and reach us only when the image ships a rebuilt kubectl (v26.7.1, the current latest, does not). Decided 2026-08-22: if the review date passes with no such image, this becomes accepted-risk under the same model -- the blast radius, if inertness is wrong, includes the Docker socket and the Galaxy token -- rather than disabling seven presets over an uninvoked library. |
| `ghcr.io/ansible/community-ansible-dev-tools:v26.7.1@sha256:4d4db3e75c48ce64763d26adbca58ff3f8b93a8ddae785373ac973b4f20a7d92` | GHSA-pwhc-rpq9-4c8w | HIGH | awaiting-upstream | 2026-09-06 | runc, containerd, docker/docker and selinux are vendored into /usr/local/bin/kubectl, which no preset invokes; the vulnerable paths need a container to be run by that binary, and kubectl runs none. Fixed versions exist at module level and reach us only when the image ships a rebuilt kubectl (v26.7.1, the current latest, does not). Decided 2026-08-22: if the review date passes with no such image, this becomes accepted-risk under the same model -- the blast radius, if inertness is wrong, includes the Docker socket and the Galaxy token -- rather than disabling seven presets over an uninvoked library. |
| `ghcr.io/ansible/community-ansible-dev-tools:v26.7.1@sha256:4d4db3e75c48ce64763d26adbca58ff3f8b93a8ddae785373ac973b4f20a7d92` | GO-2026-5064 | HIGH | awaiting-upstream | 2026-09-06 | runc, containerd, docker/docker and selinux are vendored into /usr/local/bin/kubectl, which no preset invokes; the vulnerable paths need a container to be run by that binary, and kubectl runs none. Fixed versions exist at module level and reach us only when the image ships a rebuilt kubectl (v26.7.1, the current latest, does not). Decided 2026-08-22: if the review date passes with no such image, this becomes accepted-risk under the same model -- the blast radius, if inertness is wrong, includes the Docker socket and the Galaxy token -- rather than disabling seven presets over an uninvoked library. |
| `ghcr.io/ansible/community-ansible-dev-tools:v26.7.1@sha256:4d4db3e75c48ce64763d26adbca58ff3f8b93a8ddae785373ac973b4f20a7d92` | GO-2026-5338 | HIGH | awaiting-upstream | 2026-09-06 | runc, containerd, docker/docker and selinux are vendored into /usr/local/bin/kubectl, which no preset invokes; the vulnerable paths need a container to be run by that binary, and kubectl runs none. Fixed versions exist at module level and reach us only when the image ships a rebuilt kubectl (v26.7.1, the current latest, does not). Decided 2026-08-22: if the review date passes with no such image, this becomes accepted-risk under the same model -- the blast radius, if inertness is wrong, includes the Docker socket and the Galaxy token -- rather than disabling seven presets over an uninvoked library. |
| `ghcr.io/probatum-org/probatum:0.8.0@sha256:b1f7a2bb4c1a44a29ee62b92fca4c6f6fb60fbc56843421dcd65c430d3697f51` | CVE-2026-14456 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `ghcr.io/securego/gosec:2.28.0@sha256:4342ad119a7c69f3f4e4ce78d81ba183dc774a70a7a4c6eeb15fe9e511f214f0` | CVE-2025-69649 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `ghcr.io/securego/gosec:2.28.0@sha256:4342ad119a7c69f3f4e4ce78d81ba183dc774a70a7a4c6eeb15fe9e511f214f0` | CVE-2025-69650 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `ghcr.io/securego/gosec:2.28.0@sha256:4342ad119a7c69f3f4e4ce78d81ba183dc774a70a7a4c6eeb15fe9e511f214f0` | CVE-2026-14456 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `ghcr.io/securego/gosec:2.28.0@sha256:4342ad119a7c69f3f4e4ce78d81ba183dc774a70a7a4c6eeb15fe9e511f214f0` | CVE-2026-3441 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `ghcr.io/securego/gosec:2.28.0@sha256:4342ad119a7c69f3f4e4ce78d81ba183dc774a70a7a4c6eeb15fe9e511f214f0` | CVE-2026-3442 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `ghcr.io/securego/gosec:2.28.0@sha256:4342ad119a7c69f3f4e4ce78d81ba183dc774a70a7a4c6eeb15fe9e511f214f0` | CVE-2026-6846 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `golangci/golangci-lint:v2.12.2-alpine@sha256:91b27804074a0bacea298707f016911e60cf0cdbc6c7bf5ccacb5f0606d18d60` | CVE-2025-69649 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `golangci/golangci-lint:v2.12.2-alpine@sha256:91b27804074a0bacea298707f016911e60cf0cdbc6c7bf5ccacb5f0606d18d60` | CVE-2025-69650 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `golangci/golangci-lint:v2.12.2-alpine@sha256:91b27804074a0bacea298707f016911e60cf0cdbc6c7bf5ccacb5f0606d18d60` | CVE-2026-14456 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `golangci/golangci-lint:v2.12.2-alpine@sha256:91b27804074a0bacea298707f016911e60cf0cdbc6c7bf5ccacb5f0606d18d60` | CVE-2026-3298 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `golangci/golangci-lint:v2.12.2-alpine@sha256:91b27804074a0bacea298707f016911e60cf0cdbc6c7bf5ccacb5f0606d18d60` | CVE-2026-3441 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `golangci/golangci-lint:v2.12.2-alpine@sha256:91b27804074a0bacea298707f016911e60cf0cdbc6c7bf5ccacb5f0606d18d60` | CVE-2026-3442 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `golangci/golangci-lint:v2.12.2-alpine@sha256:91b27804074a0bacea298707f016911e60cf0cdbc6c7bf5ccacb5f0606d18d60` | CVE-2026-4224 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `golangci/golangci-lint:v2.12.2-alpine@sha256:91b27804074a0bacea298707f016911e60cf0cdbc6c7bf5ccacb5f0606d18d60` | CVE-2026-6846 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `golangci/golangci-lint:v2.12.2-alpine@sha256:91b27804074a0bacea298707f016911e60cf0cdbc6c7bf5ccacb5f0606d18d60` | CVE-2026-9669 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `goreleaser/goreleaser:v2.17.0@sha256:054eefd282c02233a2556ce2d1a60cd2f51dc565ffc2520dc38b5deb4dd1ad30` | CVE-2025-69649 | HIGH | accepted-risk | 2026-11-18 | goreleaser and the buildx plugin are static: the image's OpenSSL and binutils are not in their process (binutils is only reached by a cgo build the project chose). The docker/docker CVEs are daemon-side handlers reported against the client library the buildx plugin vendors; the daemon is the host's, through the socket. No fix at any version. |
| `goreleaser/goreleaser:v2.17.0@sha256:054eefd282c02233a2556ce2d1a60cd2f51dc565ffc2520dc38b5deb4dd1ad30` | CVE-2025-69650 | HIGH | accepted-risk | 2026-11-18 | goreleaser and the buildx plugin are static: the image's OpenSSL and binutils are not in their process (binutils is only reached by a cgo build the project chose). The docker/docker CVEs are daemon-side handlers reported against the client library the buildx plugin vendors; the daemon is the host's, through the socket. No fix at any version. |
| `goreleaser/goreleaser:v2.17.0@sha256:054eefd282c02233a2556ce2d1a60cd2f51dc565ffc2520dc38b5deb4dd1ad30` | CVE-2026-14456 | HIGH | accepted-risk | 2026-11-18 | goreleaser and the buildx plugin are static: the image's OpenSSL and binutils are not in their process (binutils is only reached by a cgo build the project chose). The docker/docker CVEs are daemon-side handlers reported against the client library the buildx plugin vendors; the daemon is the host's, through the socket. No fix at any version. |
| `goreleaser/goreleaser:v2.17.0@sha256:054eefd282c02233a2556ce2d1a60cd2f51dc565ffc2520dc38b5deb4dd1ad30` | CVE-2026-3441 | HIGH | accepted-risk | 2026-11-18 | goreleaser and the buildx plugin are static: the image's OpenSSL and binutils are not in their process (binutils is only reached by a cgo build the project chose). The docker/docker CVEs are daemon-side handlers reported against the client library the buildx plugin vendors; the daemon is the host's, through the socket. No fix at any version. |
| `goreleaser/goreleaser:v2.17.0@sha256:054eefd282c02233a2556ce2d1a60cd2f51dc565ffc2520dc38b5deb4dd1ad30` | CVE-2026-3442 | HIGH | accepted-risk | 2026-11-18 | goreleaser and the buildx plugin are static: the image's OpenSSL and binutils are not in their process (binutils is only reached by a cgo build the project chose). The docker/docker CVEs are daemon-side handlers reported against the client library the buildx plugin vendors; the daemon is the host's, through the socket. No fix at any version. |
| `goreleaser/goreleaser:v2.17.0@sha256:054eefd282c02233a2556ce2d1a60cd2f51dc565ffc2520dc38b5deb4dd1ad30` | CVE-2026-41567 | HIGH | accepted-risk | 2026-11-18 | goreleaser and the buildx plugin are static: the image's OpenSSL and binutils are not in their process (binutils is only reached by a cgo build the project chose). The docker/docker CVEs are daemon-side handlers reported against the client library the buildx plugin vendors; the daemon is the host's, through the socket. No fix at any version. |
| `goreleaser/goreleaser:v2.17.0@sha256:054eefd282c02233a2556ce2d1a60cd2f51dc565ffc2520dc38b5deb4dd1ad30` | CVE-2026-42306 | HIGH | accepted-risk | 2026-11-18 | goreleaser and the buildx plugin are static: the image's OpenSSL and binutils are not in their process (binutils is only reached by a cgo build the project chose). The docker/docker CVEs are daemon-side handlers reported against the client library the buildx plugin vendors; the daemon is the host's, through the socket. No fix at any version. |
| `goreleaser/goreleaser:v2.17.0@sha256:054eefd282c02233a2556ce2d1a60cd2f51dc565ffc2520dc38b5deb4dd1ad30` | CVE-2026-6846 | HIGH | accepted-risk | 2026-11-18 | goreleaser and the buildx plugin are static: the image's OpenSSL and binutils are not in their process (binutils is only reached by a cgo build the project chose). The docker/docker CVEs are daemon-side handlers reported against the client library the buildx plugin vendors; the daemon is the host's, through the socket. No fix at any version. |
| `jauderho/prettier:3.9.4@sha256:b233d9c847048561850a1a107e35318b28361cd9a850eac7edcd5d8bfd82cea2` | CVE-2026-14456 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `jauderho/prettier:3.9.4@sha256:b233d9c847048561850a1a107e35318b28361cd9a850eac7edcd5d8bfd82cea2` | CVE-2026-48615 | HIGH | accepted-risk | 2026-11-18 | This package IS what the preset runs, so the 'nothing calls it' argument does not apply. What bounds it instead is the input: the tool reads the repository's own source, or fetches a URL the preset itself fixes -- never bytes chosen by anyone else. Weaker than the inert case and stated as such. No fix at any version. |
| `jauderho/prettier:3.9.4@sha256:b233d9c847048561850a1a107e35318b28361cd9a850eac7edcd5d8bfd82cea2` | CVE-2026-48619 | HIGH | accepted-risk | 2026-11-18 | This package IS what the preset runs, so the 'nothing calls it' argument does not apply. What bounds it instead is the input: the tool reads the repository's own source, or fetches a URL the preset itself fixes -- never bytes chosen by anyone else. Weaker than the inert case and stated as such. No fix at any version. |
| `jauderho/prettier:3.9.4@sha256:b233d9c847048561850a1a107e35318b28361cd9a850eac7edcd5d8bfd82cea2` | CVE-2026-48930 | HIGH | accepted-risk | 2026-11-18 | This package IS what the preset runs, so the 'nothing calls it' argument does not apply. What bounds it instead is the input: the tool reads the repository's own source, or fetches a URL the preset itself fixes -- never bytes chosen by anyone else. Weaker than the inert case and stated as such. No fix at any version. |
| `jauderho/prettier:3.9.4@sha256:b233d9c847048561850a1a107e35318b28361cd9a850eac7edcd5d8bfd82cea2` | CVE-2026-48933 | HIGH | accepted-risk | 2026-11-18 | This package IS what the preset runs, so the 'nothing calls it' argument does not apply. What bounds it instead is the input: the tool reads the repository's own source, or fetches a URL the preset itself fixes -- never bytes chosen by anyone else. Weaker than the inert case and stated as such. No fix at any version. |
| `jauderho/prettier:3.9.4@sha256:b233d9c847048561850a1a107e35318b28361cd9a850eac7edcd5d8bfd82cea2` | CVE-2026-56848 | HIGH | accepted-risk | 2026-11-18 | This package IS what the preset runs, so the 'nothing calls it' argument does not apply. What bounds it instead is the input: the tool reads the repository's own source, or fetches a URL the preset itself fixes -- never bytes chosen by anyone else. Weaker than the inert case and stated as such. No fix at any version. |
| `jauderho/prettier:3.9.4@sha256:b233d9c847048561850a1a107e35318b28361cd9a850eac7edcd5d8bfd82cea2` | CVE-2026-58043 | HIGH | accepted-risk | 2026-11-18 | This package IS what the preset runs, so the 'nothing calls it' argument does not apply. What bounds it instead is the input: the tool reads the repository's own source, or fetches a URL the preset itself fixes -- never bytes chosen by anyone else. Weaker than the inert case and stated as such. No fix at any version. |
| `maniator/gh:v2.95@sha256:c7e862eeb468003aba87847a03e214a21638cd898130d6e5894f019aec091913` | CVE-2026-14456 | HIGH | accepted-risk | 2026-11-18 | gh is static; the image's OpenSSL is linked by nothing the preset runs (git is present and used locally only). No fix at any version. |
| `pyfound/black:26.5.1@sha256:bcdafe3e6a60fd181fde19859f7ee4c498557f03bb30af3fa880f502a66e5b5f` | CVE-2025-69720 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `pyfound/black:26.5.1@sha256:bcdafe3e6a60fd181fde19859f7ee4c498557f03bb30af3fa880f502a66e5b5f` | CVE-2026-11822 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `pyfound/black:26.5.1@sha256:bcdafe3e6a60fd181fde19859f7ee4c498557f03bb30af3fa880f502a66e5b5f` | CVE-2026-11824 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `pyfound/black:26.5.1@sha256:bcdafe3e6a60fd181fde19859f7ee4c498557f03bb30af3fa880f502a66e5b5f` | CVE-2026-12087 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `pyfound/black:26.5.1@sha256:bcdafe3e6a60fd181fde19859f7ee4c498557f03bb30af3fa880f502a66e5b5f` | CVE-2026-13221 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `pyfound/black:26.5.1@sha256:bcdafe3e6a60fd181fde19859f7ee4c498557f03bb30af3fa880f502a66e5b5f` | CVE-2026-14456 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `pyfound/black:26.5.1@sha256:bcdafe3e6a60fd181fde19859f7ee4c498557f03bb30af3fa880f502a66e5b5f` | CVE-2026-41992 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `pyfound/black:26.5.1@sha256:bcdafe3e6a60fd181fde19859f7ee4c498557f03bb30af3fa880f502a66e5b5f` | CVE-2026-42496 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `pyfound/black:26.5.1@sha256:bcdafe3e6a60fd181fde19859f7ee4c498557f03bb30af3fa880f502a66e5b5f` | CVE-2026-42497 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `pyfound/black:26.5.1@sha256:bcdafe3e6a60fd181fde19859f7ee4c498557f03bb30af3fa880f502a66e5b5f` | CVE-2026-48959 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `pyfound/black:26.5.1@sha256:bcdafe3e6a60fd181fde19859f7ee4c498557f03bb30af3fa880f502a66e5b5f` | CVE-2026-48961 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `pyfound/black:26.5.1@sha256:bcdafe3e6a60fd181fde19859f7ee4c498557f03bb30af3fa880f502a66e5b5f` | CVE-2026-48962 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `pyfound/black:26.5.1@sha256:bcdafe3e6a60fd181fde19859f7ee4c498557f03bb30af3fa880f502a66e5b5f` | CVE-2026-5435 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `pyfound/black:26.5.1@sha256:bcdafe3e6a60fd181fde19859f7ee4c498557f03bb30af3fa880f502a66e5b5f` | CVE-2026-54369 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `pyfound/black:26.5.1@sha256:bcdafe3e6a60fd181fde19859f7ee4c498557f03bb30af3fa880f502a66e5b5f` | CVE-2026-54370 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `pyfound/black:26.5.1@sha256:bcdafe3e6a60fd181fde19859f7ee4c498557f03bb30af3fa880f502a66e5b5f` | CVE-2026-5450 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `pyfound/black:26.5.1@sha256:bcdafe3e6a60fd181fde19859f7ee4c498557f03bb30af3fa880f502a66e5b5f` | CVE-2026-57432 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `pyfound/black:26.5.1@sha256:bcdafe3e6a60fd181fde19859f7ee4c498557f03bb30af3fa880f502a66e5b5f` | CVE-2026-57433 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `pyfound/black:26.5.1@sha256:bcdafe3e6a60fd181fde19859f7ee4c498557f03bb30af3fa880f502a66e5b5f` | CVE-2026-5928 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `pyfound/black:26.5.1@sha256:bcdafe3e6a60fd181fde19859f7ee4c498557f03bb30af3fa880f502a66e5b5f` | CVE-2026-7017 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `pyfound/black:26.5.1@sha256:bcdafe3e6a60fd181fde19859f7ee4c498557f03bb30af3fa880f502a66e5b5f` | CVE-2026-8376 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `pyfound/black:26.5.1@sha256:bcdafe3e6a60fd181fde19859f7ee4c498557f03bb30af3fa880f502a66e5b5f` | CVE-2026-9538 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
| `rust:1.97.1-slim@sha256:3b2879047d42784ca9403ad20c51ed3df361a50f1df96f5777d39b4e33aa65cd` | CVE-2013-7445 | HIGH | mitigated | 2026-11-06 | Kernel CVE flagged on linux-libc-dev, the kernel headers of the Debian base. A container executes the host's kernel, never the image's headers, and Debian ships no fix in stable. Re-argued 2026-08-06 against audit run 31085058431 |
| `rust:1.97.1-slim@sha256:3b2879047d42784ca9403ad20c51ed3df361a50f1df96f5777d39b4e33aa65cd` | CVE-2019-19449 | HIGH | mitigated | 2026-11-06 | Kernel CVE flagged on linux-libc-dev, the kernel headers of the Debian base. A container executes the host's kernel, never the image's headers, and Debian ships no fix in stable. Re-argued 2026-08-06 against audit run 31085058431 |
| `rust:1.97.1-slim@sha256:3b2879047d42784ca9403ad20c51ed3df361a50f1df96f5777d39b4e33aa65cd` | CVE-2019-19814 | HIGH | mitigated | 2026-11-06 | Kernel CVE flagged on linux-libc-dev, the kernel headers of the Debian base. A container executes the host's kernel, never the image's headers, and Debian ships no fix in stable. Re-argued 2026-08-06 against audit run 31085058431 |
| `rust:1.97.1-slim@sha256:3b2879047d42784ca9403ad20c51ed3df361a50f1df96f5777d39b4e33aa65cd` | CVE-2021-3847 | HIGH | mitigated | 2026-11-06 | Kernel CVE flagged on linux-libc-dev, the kernel headers of the Debian base. A container executes the host's kernel, never the image's headers, and Debian ships no fix in stable. Re-argued 2026-08-06 against audit run 31085058431 |
| `rust:1.97.1-slim@sha256:3b2879047d42784ca9403ad20c51ed3df361a50f1df96f5777d39b4e33aa65cd` | CVE-2021-3864 | HIGH | mitigated | 2026-11-06 | Kernel CVE flagged on linux-libc-dev, the kernel headers of the Debian base. A container executes the host's kernel, never the image's headers, and Debian ships no fix in stable. Re-argued 2026-08-06 against audit run 31085058431 |
| `rust:1.97.1-slim@sha256:3b2879047d42784ca9403ad20c51ed3df361a50f1df96f5777d39b4e33aa65cd` | CVE-2024-21803 | HIGH | mitigated | 2026-11-06 | Kernel CVE flagged on linux-libc-dev, the kernel headers of the Debian base. A container executes the host's kernel, never the image's headers, and Debian ships no fix in stable. Re-argued 2026-08-06 against audit run 31085058431 |
| `rust:1.97.1-slim@sha256:3b2879047d42784ca9403ad20c51ed3df361a50f1df96f5777d39b4e33aa65cd` | CVE-2024-58015 | HIGH | mitigated | 2026-11-06 | Kernel CVE flagged on linux-libc-dev, the kernel headers of the Debian base. A container executes the host's kernel, never the image's headers, and Debian ships no fix in stable. Re-argued 2026-08-06 against audit run 31085058431 |
| `rust:1.97.1-slim@sha256:3b2879047d42784ca9403ad20c51ed3df361a50f1df96f5777d39b4e33aa65cd` | CVE-2025-22104 | HIGH | mitigated | 2026-11-06 | Kernel CVE flagged on linux-libc-dev, the kernel headers of the Debian base. A container executes the host's kernel, never the image's headers, and Debian ships no fix in stable. Re-argued 2026-08-06 against audit run 31085058431 |
| `rust:1.97.1-slim@sha256:3b2879047d42784ca9403ad20c51ed3df361a50f1df96f5777d39b4e33aa65cd` | CVE-2025-38137 | HIGH | mitigated | 2026-11-06 | Kernel CVE flagged on linux-libc-dev, the kernel headers of the Debian base. A container executes the host's kernel, never the image's headers, and Debian ships no fix in stable. Re-argued 2026-08-06 against audit run 31085058431 |
| `rust:1.97.1-slim@sha256:3b2879047d42784ca9403ad20c51ed3df361a50f1df96f5777d39b4e33aa65cd` | CVE-2025-38187 | HIGH | mitigated | 2026-11-06 | Kernel CVE flagged on linux-libc-dev, the kernel headers of the Debian base. A container executes the host's kernel, never the image's headers, and Debian ships no fix in stable. Re-argued 2026-08-06 against audit run 31085058431 |
| `rust:1.97.1-slim@sha256:3b2879047d42784ca9403ad20c51ed3df361a50f1df96f5777d39b4e33aa65cd` | CVE-2025-38421 | HIGH | mitigated | 2026-11-06 | Kernel CVE flagged on linux-libc-dev, the kernel headers of the Debian base. A container executes the host's kernel, never the image's headers, and Debian ships no fix in stable. Re-argued 2026-08-06 against audit run 31085058431 |
| `rust:1.97.1-slim@sha256:3b2879047d42784ca9403ad20c51ed3df361a50f1df96f5777d39b4e33aa65cd` | CVE-2025-38636 | HIGH | mitigated | 2026-11-06 | Kernel CVE flagged on linux-libc-dev, the kernel headers of the Debian base. A container executes the host's kernel, never the image's headers, and Debian ships no fix in stable. Re-argued 2026-08-06 against audit run 31085058431 |
| `rust:1.97.1-slim@sha256:3b2879047d42784ca9403ad20c51ed3df361a50f1df96f5777d39b4e33aa65cd` | CVE-2025-69720 | HIGH | accepted-risk | 2026-11-18 | perl, gzip, sqlite, tinfo, acl and the image's OpenSSL are neither linked by cargo/rustc nor invoked by any preset command (probed on the pinned digest, audit run 32557245489); glibc is linked, and the flagged paths (ns_printrr, scanf %mc, ungetwc) are not ones Rust std uses. cargo-publish's token travels through cargo's own TLS. No fix at any version. |
| `rust:1.97.1-slim@sha256:3b2879047d42784ca9403ad20c51ed3df361a50f1df96f5777d39b4e33aa65cd` | CVE-2026-11822 | HIGH | accepted-risk | 2026-11-18 | perl, gzip, sqlite, tinfo, acl and the image's OpenSSL are neither linked by cargo/rustc nor invoked by any preset command (probed on the pinned digest, audit run 32557245489); glibc is linked, and the flagged paths (ns_printrr, scanf %mc, ungetwc) are not ones Rust std uses. cargo-publish's token travels through cargo's own TLS. No fix at any version. |
| `rust:1.97.1-slim@sha256:3b2879047d42784ca9403ad20c51ed3df361a50f1df96f5777d39b4e33aa65cd` | CVE-2026-11824 | HIGH | accepted-risk | 2026-11-18 | perl, gzip, sqlite, tinfo, acl and the image's OpenSSL are neither linked by cargo/rustc nor invoked by any preset command (probed on the pinned digest, audit run 32557245489); glibc is linked, and the flagged paths (ns_printrr, scanf %mc, ungetwc) are not ones Rust std uses. cargo-publish's token travels through cargo's own TLS. No fix at any version. |
| `rust:1.97.1-slim@sha256:3b2879047d42784ca9403ad20c51ed3df361a50f1df96f5777d39b4e33aa65cd` | CVE-2026-12087 | HIGH | accepted-risk | 2026-11-18 | perl, gzip, sqlite, tinfo, acl and the image's OpenSSL are neither linked by cargo/rustc nor invoked by any preset command (probed on the pinned digest, audit run 32557245489); glibc is linked, and the flagged paths (ns_printrr, scanf %mc, ungetwc) are not ones Rust std uses. cargo-publish's token travels through cargo's own TLS. No fix at any version. |
| `rust:1.97.1-slim@sha256:3b2879047d42784ca9403ad20c51ed3df361a50f1df96f5777d39b4e33aa65cd` | CVE-2026-13221 | HIGH | accepted-risk | 2026-11-18 | perl, gzip, sqlite, tinfo, acl and the image's OpenSSL are neither linked by cargo/rustc nor invoked by any preset command (probed on the pinned digest, audit run 32557245489); glibc is linked, and the flagged paths (ns_printrr, scanf %mc, ungetwc) are not ones Rust std uses. cargo-publish's token travels through cargo's own TLS. No fix at any version. |
| `rust:1.97.1-slim@sha256:3b2879047d42784ca9403ad20c51ed3df361a50f1df96f5777d39b4e33aa65cd` | CVE-2026-14456 | HIGH | accepted-risk | 2026-11-18 | perl, gzip, sqlite, tinfo, acl and the image's OpenSSL are neither linked by cargo/rustc nor invoked by any preset command (probed on the pinned digest, audit run 32557245489); glibc is linked, and the flagged paths (ns_printrr, scanf %mc, ungetwc) are not ones Rust std uses. cargo-publish's token travels through cargo's own TLS. No fix at any version. |
| `rust:1.97.1-slim@sha256:3b2879047d42784ca9403ad20c51ed3df361a50f1df96f5777d39b4e33aa65cd` | CVE-2026-41992 | HIGH | accepted-risk | 2026-11-18 | perl, gzip, sqlite, tinfo, acl and the image's OpenSSL are neither linked by cargo/rustc nor invoked by any preset command (probed on the pinned digest, audit run 32557245489); glibc is linked, and the flagged paths (ns_printrr, scanf %mc, ungetwc) are not ones Rust std uses. cargo-publish's token travels through cargo's own TLS. No fix at any version. |
| `rust:1.97.1-slim@sha256:3b2879047d42784ca9403ad20c51ed3df361a50f1df96f5777d39b4e33aa65cd` | CVE-2026-42496 | HIGH | accepted-risk | 2026-11-18 | perl, gzip, sqlite, tinfo, acl and the image's OpenSSL are neither linked by cargo/rustc nor invoked by any preset command (probed on the pinned digest, audit run 32557245489); glibc is linked, and the flagged paths (ns_printrr, scanf %mc, ungetwc) are not ones Rust std uses. cargo-publish's token travels through cargo's own TLS. No fix at any version. |
| `rust:1.97.1-slim@sha256:3b2879047d42784ca9403ad20c51ed3df361a50f1df96f5777d39b4e33aa65cd` | CVE-2026-42497 | HIGH | accepted-risk | 2026-11-18 | perl, gzip, sqlite, tinfo, acl and the image's OpenSSL are neither linked by cargo/rustc nor invoked by any preset command (probed on the pinned digest, audit run 32557245489); glibc is linked, and the flagged paths (ns_printrr, scanf %mc, ungetwc) are not ones Rust std uses. cargo-publish's token travels through cargo's own TLS. No fix at any version. |
| `rust:1.97.1-slim@sha256:3b2879047d42784ca9403ad20c51ed3df361a50f1df96f5777d39b4e33aa65cd` | CVE-2026-48959 | HIGH | accepted-risk | 2026-11-18 | perl, gzip, sqlite, tinfo, acl and the image's OpenSSL are neither linked by cargo/rustc nor invoked by any preset command (probed on the pinned digest, audit run 32557245489); glibc is linked, and the flagged paths (ns_printrr, scanf %mc, ungetwc) are not ones Rust std uses. cargo-publish's token travels through cargo's own TLS. No fix at any version. |
| `rust:1.97.1-slim@sha256:3b2879047d42784ca9403ad20c51ed3df361a50f1df96f5777d39b4e33aa65cd` | CVE-2026-48961 | HIGH | accepted-risk | 2026-11-18 | perl, gzip, sqlite, tinfo, acl and the image's OpenSSL are neither linked by cargo/rustc nor invoked by any preset command (probed on the pinned digest, audit run 32557245489); glibc is linked, and the flagged paths (ns_printrr, scanf %mc, ungetwc) are not ones Rust std uses. cargo-publish's token travels through cargo's own TLS. No fix at any version. |
| `rust:1.97.1-slim@sha256:3b2879047d42784ca9403ad20c51ed3df361a50f1df96f5777d39b4e33aa65cd` | CVE-2026-48962 | HIGH | accepted-risk | 2026-11-18 | perl, gzip, sqlite, tinfo, acl and the image's OpenSSL are neither linked by cargo/rustc nor invoked by any preset command (probed on the pinned digest, audit run 32557245489); glibc is linked, and the flagged paths (ns_printrr, scanf %mc, ungetwc) are not ones Rust std uses. cargo-publish's token travels through cargo's own TLS. No fix at any version. |
| `rust:1.97.1-slim@sha256:3b2879047d42784ca9403ad20c51ed3df361a50f1df96f5777d39b4e33aa65cd` | CVE-2026-5435 | HIGH | accepted-risk | 2026-11-18 | perl, gzip, sqlite, tinfo, acl and the image's OpenSSL are neither linked by cargo/rustc nor invoked by any preset command (probed on the pinned digest, audit run 32557245489); glibc is linked, and the flagged paths (ns_printrr, scanf %mc, ungetwc) are not ones Rust std uses. cargo-publish's token travels through cargo's own TLS. No fix at any version. |
| `rust:1.97.1-slim@sha256:3b2879047d42784ca9403ad20c51ed3df361a50f1df96f5777d39b4e33aa65cd` | CVE-2026-54369 | HIGH | accepted-risk | 2026-11-18 | perl, gzip, sqlite, tinfo, acl and the image's OpenSSL are neither linked by cargo/rustc nor invoked by any preset command (probed on the pinned digest, audit run 32557245489); glibc is linked, and the flagged paths (ns_printrr, scanf %mc, ungetwc) are not ones Rust std uses. cargo-publish's token travels through cargo's own TLS. No fix at any version. |
| `rust:1.97.1-slim@sha256:3b2879047d42784ca9403ad20c51ed3df361a50f1df96f5777d39b4e33aa65cd` | CVE-2026-54370 | HIGH | accepted-risk | 2026-11-18 | perl, gzip, sqlite, tinfo, acl and the image's OpenSSL are neither linked by cargo/rustc nor invoked by any preset command (probed on the pinned digest, audit run 32557245489); glibc is linked, and the flagged paths (ns_printrr, scanf %mc, ungetwc) are not ones Rust std uses. cargo-publish's token travels through cargo's own TLS. No fix at any version. |
| `rust:1.97.1-slim@sha256:3b2879047d42784ca9403ad20c51ed3df361a50f1df96f5777d39b4e33aa65cd` | CVE-2026-5450 | HIGH | accepted-risk | 2026-11-18 | perl, gzip, sqlite, tinfo, acl and the image's OpenSSL are neither linked by cargo/rustc nor invoked by any preset command (probed on the pinned digest, audit run 32557245489); glibc is linked, and the flagged paths (ns_printrr, scanf %mc, ungetwc) are not ones Rust std uses. cargo-publish's token travels through cargo's own TLS. No fix at any version. |
| `rust:1.97.1-slim@sha256:3b2879047d42784ca9403ad20c51ed3df361a50f1df96f5777d39b4e33aa65cd` | CVE-2026-57432 | HIGH | accepted-risk | 2026-11-18 | perl, gzip, sqlite, tinfo, acl and the image's OpenSSL are neither linked by cargo/rustc nor invoked by any preset command (probed on the pinned digest, audit run 32557245489); glibc is linked, and the flagged paths (ns_printrr, scanf %mc, ungetwc) are not ones Rust std uses. cargo-publish's token travels through cargo's own TLS. No fix at any version. |
| `rust:1.97.1-slim@sha256:3b2879047d42784ca9403ad20c51ed3df361a50f1df96f5777d39b4e33aa65cd` | CVE-2026-57433 | HIGH | accepted-risk | 2026-11-18 | perl, gzip, sqlite, tinfo, acl and the image's OpenSSL are neither linked by cargo/rustc nor invoked by any preset command (probed on the pinned digest, audit run 32557245489); glibc is linked, and the flagged paths (ns_printrr, scanf %mc, ungetwc) are not ones Rust std uses. cargo-publish's token travels through cargo's own TLS. No fix at any version. |
| `rust:1.97.1-slim@sha256:3b2879047d42784ca9403ad20c51ed3df361a50f1df96f5777d39b4e33aa65cd` | CVE-2026-5928 | HIGH | accepted-risk | 2026-11-18 | perl, gzip, sqlite, tinfo, acl and the image's OpenSSL are neither linked by cargo/rustc nor invoked by any preset command (probed on the pinned digest, audit run 32557245489); glibc is linked, and the flagged paths (ns_printrr, scanf %mc, ungetwc) are not ones Rust std uses. cargo-publish's token travels through cargo's own TLS. No fix at any version. |
| `rust:1.97.1-slim@sha256:3b2879047d42784ca9403ad20c51ed3df361a50f1df96f5777d39b4e33aa65cd` | CVE-2026-7017 | HIGH | accepted-risk | 2026-11-18 | perl, gzip, sqlite, tinfo, acl and the image's OpenSSL are neither linked by cargo/rustc nor invoked by any preset command (probed on the pinned digest, audit run 32557245489); glibc is linked, and the flagged paths (ns_printrr, scanf %mc, ungetwc) are not ones Rust std uses. cargo-publish's token travels through cargo's own TLS. No fix at any version. |
| `rust:1.97.1-slim@sha256:3b2879047d42784ca9403ad20c51ed3df361a50f1df96f5777d39b4e33aa65cd` | CVE-2026-8376 | HIGH | accepted-risk | 2026-11-18 | perl, gzip, sqlite, tinfo, acl and the image's OpenSSL are neither linked by cargo/rustc nor invoked by any preset command (probed on the pinned digest, audit run 32557245489); glibc is linked, and the flagged paths (ns_printrr, scanf %mc, ungetwc) are not ones Rust std uses. cargo-publish's token travels through cargo's own TLS. No fix at any version. |
| `rust:1.97.1-slim@sha256:3b2879047d42784ca9403ad20c51ed3df361a50f1df96f5777d39b4e33aa65cd` | CVE-2026-9538 | HIGH | accepted-risk | 2026-11-18 | perl, gzip, sqlite, tinfo, acl and the image's OpenSSL are neither linked by cargo/rustc nor invoked by any preset command (probed on the pinned digest, audit run 32557245489); glibc is linked, and the flagged paths (ns_printrr, scanf %mc, ungetwc) are not ones Rust std uses. cargo-publish's token travels through cargo's own TLS. No fix at any version. |
| `zricethezav/gitleaks:v8.30.1@sha256:c00b6bd0aeb3071cbcb79009cb16a60dd9e0a7c60e2be9ab65d25e6bc8abbb7f` | CVE-2026-14456 | HIGH | accepted-risk | 2026-11-18 | Present in the image, invoked by nothing this preset runs. A CIDX container lives for the length of one phase, exposes no service, receives no attacker-controlled input and persists nothing, so a flaw in a package no code path reaches has no way to be exercised here. No fix at any version; re-argue if the preset's command ever starts calling it. |
